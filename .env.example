# Notion integration token (same one the meal planner uses) and the Recipes database id
NOTION_TOKEN=
NOTION_RECIPE_DATA_SOURCE_ID=

# Where the meal planner API lives (no trailing slash). Leave empty to run the tour standalone.
PLANNER_URL=https://atlas.tail77fe3.ts.net/meal-planner

API_PORT=5181
