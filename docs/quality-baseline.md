# Quality baseline

The numbers below were measured on 2026-09-15 after the room-loop, mask and Black Sea passes. A coordinating agent reads them at session start, re-measures with the named commands, and updates this file when a number changes on purpose. A number that drops without an entry here is a regression.

| Measure | Value | Command |
| --- | --- | --- |
| Type check | Passes | `npm run typecheck` |
| Test harnesses | 16 pass | `npm test` |
| Painted rooms | 66, all with three or four always-on loops in wide and portrait | `node scripts/tests/room-loops.mjs` |
| Steam sources | Every configured source draws, cap four per room | `scene-painted.ts`, `steamLimit` |
| Visible idle motion | Rooms with hot food or mist change 3 percent or more of the frame in two seconds; outdoor rooms at least 2.5 percent or a medium crisp cue. Weakest after the 2026-09-15 pass: olive grove, oasis field, oasis bazaar. Spain, measured through the world route on 2026-09-16: every room 3.2 percent or more wide and 3.55 or more portrait, weakest the fair cauldron wide. **Stale for eight Spain rooms** since the breeze crops came out later that day; see the change entry below | `scripts/audit/room-motion.py` after capturing pairs |
| Object ids | Unique across all worlds | `node scripts/tests/object-ids.mjs` |
| Breeze masks | 30 crops, all China, Xinjiang and Turkey, each inspected; none moves a face, wall, lantern, shelf or pole. Spain uses none: a painted string is either painted out of the picture and replaced by a hung sprite, or left still | `uv run --with pillow --with numpy --with scipy scripts/audit/breeze-masks.py <out dir>` |
| Derived room pictures | 4: `es_tortilla` and `es_manchego`, wide and portrait, have their pepper string painted out. Originals in `.data/originals/<room>/<orientation>.jpg` with a SHA-256 sidecar; `.data` is outside git, so the reproducible chain is the delivered PNGs (hashes in `public/scenes/spain-assets.json`) → `import-spain.py` → `paint-out-strings.py` | `uv run --with pillow --with numpy --with scipy --with opencv-python-headless scripts/scenes/paint-out-strings.py` (`--restore` undoes it) |
| Card-only clickables per area | Sichuan 9, Jiangnan 6, Northern 5, Xinjiang 4, Everyday 1, Istanbul 5, Anatolia 3, Aegean 2, Black Sea 3, Levant 7, Arabia 3, Persia 2, Spain 14, Greece 4, Morocco 3, Dalmatia 2 | `node scripts/audit/objects.mjs` |
| Rooms per area | Sichuan 5, Jiangnan 9, Northern 12, Xinjiang 12, Everyday 1, Istanbul 7, Anatolia 4, Aegean 3, Black Sea 1, Spain 12 | `node scripts/audit/objects.mjs` |
| Published worlds | china, middle-east | `world-availability.ts` |
| Live site | https://fyyying.github.io/food-world/ deploys on every push to `main` | `gh run watch --exit-status` |

## What the harnesses enforce

These checks were written after a defect reached the owner. They live in `scripts/tests/spain-world.mjs` and `scripts/tests/room-loops.mjs` and run in `npm test`. **Every new area's `<id>-world.mjs` carries the world checks**; copy them and change the ids. A check that a new area cannot satisfy is a design decision the lead records here, not a check to drop.

| Enforced | Where | Added because |
| --- | --- | --- |
| Decorative houses stay between 12 and 16 for the area, one to three per style, bounded at both ends | `spain-world.mjs` | Twenty-four houses hemmed the stands in and closed three lanes |
| Nothing solid stands on a road centreline; every centreline is sampled every 0.25 units against every solid decor footprint. Only a bridge deck is walked over | `spain-world.mjs` | Six houses, two sheaves, a rock, an outcrop and an apple tree stood on lanes |
| Every stand keeps an open corridor from the nearest point of the road network, with 2.5 units clear in front of the stand | `spain-world.mjs` | Stands could not be walked up to |
| No house or decor stands on the arrival camera's line to a stand, from any azimuth the stand can be approached from | `spain-world.mjs`, `spain-reactions.mjs` | A three-storey house stood on the line to two stands |
| Gait: any figure whose world position travels must swing its thighs, and a figure that should stand still must not travel. A figure carried by a vehicle must be seated | `spain-world.mjs` | Ten paced walkers slid with locked legs; three sailors stood upright on sailing boats |
| Gait direction: an animal's head axis leads its travel, and its hooves spend clearly more of the stride going backward under the body than forward | `spain-world.mjs` | The mill mule circled tail-first with pendulum legs |
| Every hung lamp reaches the ground and carries its lantern above head height | `spain-world.mjs` | Six square lamps hung in the air after the arcades were removed |
| Removed decor stays removed: the arcade, the barraca and the hórreo must be absent | `spain-world.mjs` | Each read from the overview as something it is not |
| Chimney smoke anchors sit at the chimney top, computed from each style's own ridge height plus the cap; a smoking house produces at least four puff sprites and uses the tinted variant | `spain-world.mjs` | Smoke was born inside the roof geometry and was invisible at overview zoom |
| A swaying hung sprite counts as a loop, so a room that replaces a keyed crop with a sprite keeps three or four loops in both orientations | `room-loops.mjs` | The eleven Spain breeze crops were removed |

Conventions these rest on:

- **A room picture may be a derived file.** A painted object that a sprite will replace is painted out offline by `scripts/scenes/paint-out-strings.py`, which writes the original and a SHA-256 sidecar to `.data/originals/<room>/<orientation>.jpg` and refuses to run twice over the same file. `--restore` undoes it. `.data` is outside git, so the reproducible chain is the delivered PNGs (hashes in `public/scenes/<area>-assets.json`) → `import-<id>.py` → `paint-out-strings.py`. Four Spain JPGs are derived today
- **Hanging motion is a sprite over a clean painting**, the way the China hotpot room hangs its lanterns. New areas do not colour-key a crop out of a finished painting; the thirty existing crops in China, Xinjiang and Turkey stay. A breeze-mask coverage above about 0.7 means the key is taking the wall
- **Every delivered motion sprite is used, or its non-use is explained in the area's room doc**

Known gaps, accepted for now:

- Levant, Arabia, Persia, Greece, Morocco and Dalmatia have card-only objects and no rooms. They predate the China standard; the Mediterranean audit began on 2026-09-16 with Spain
- The Everyday area of China has one room and one card-only object
- Hotpot's people wear present-day clothes; the art direction exempts it as the light and density reference
- Portrait composites from `__fw.sceneShot` squash the frame in a hidden pane; judge portrait rooms in the live pane at 390 x 844

How to update: change a value, add one line under a dated heading below that says what changed and why, and commit the file with the change that caused it.

## Changes

- 2026-09-16: Documentation pass after the owner's ten-minute walkthrough of Spain. The twelve shortfalls it found are written up as rules where they belong: the room motion standard, the measure-on-the-pixels and overlay-contact-sheet rule, the patch-edge rule, the steam and boil rule, the house caps and stand corridors and the overview-read rule went into the [new-area methodology](new-area-methodology.md); the model tiering, the agent-brief format, the owner-walkthrough stage before Stage F, the confirm-before-removing rule and the new lines in the definition of done went into the [agent team playbook](agent-team-playbook.md); the [Spain world](spain-world.md) carries the retrospective. This file gained the list above of what the harnesses now enforce, which every new area's `<id>-world.mjs` must carry. No measured number changed in this pass; the eight stale Spain motion numbers are still stale and re-running `room-motion.py` through the world route is still open.
- 2026-09-15: Idle motion measured in every room after the owner could not see the tea hill's loops. Mist opacity, bird size and period, leaf count and size and beam drift became tunable; sixteen weak rooms got steam from pictured vessels, readable mist, larger birds, more leaves, sparks at the fish grill, petals at the Chinese tea hill. The Turkish bazaar object id became `bazaarTr`; it had collided with China's oasis bazaar and Turkish card links pointed at Xinjiang. `object-ids.mjs` now guards this.
- 2026-09-15: The two Anatolian wheat plots rose from .019 to .030 above the paving at .018 and the eastern one moved off the lane edge; their soil had z-fought with the paving.
- 2026-09-15: The Anatolian kitchen garden shrank from 7 to 4 units wide; it had overlapped the coppersmith's and potter's floor slabs at the same height and z-fought. `scripts/audit/flicker.mjs` now reports zero exact overlaps at both shops.
- 2026-09-15: The house south of the Istanbul coffeehouse was removed because it hid the coffeehouse from the default view; the Anatolian front house dropped to two storeys; courtyard houses no longer receive a street planter.
- 2026-09-15: Baseline created. 20 rooms raised from one or two loops to three or four; 9 masks tightened; Black Sea gained hazelnut grove, anchovy landing and kale beds; the Turkish bazaar lost its own floor plane and the stone squares rose to 0.027 to stop z-fighting.
- 2026-09-16: Spain built to the China standard in the Mediterranean world: 12 rooms, 15 card-only clickables, two new harnesses (`spain-world.mjs`, `spain-reactions.mjs`), the object audit extended to the Mediterranean, `breeze-masks.py` extended to per-world ambience files. The Mediterranean desktop zoom limit became 215 for its 120-wide table, and the room approach loosens the pitch clamp so the low approach is not cancelled. The Mediterranean stays unpublished until the owner decides about its three card-only areas.
- 2026-09-16: Owner feedback on the live Spain table, five items, all fixed. Spain's decorative houses went from 24 to 14 (one or two per style per cluster) because they hemmed the clickable stands in and stood on three lanes; `spain-world.mjs` gained two checks that read the built world, one that no solid decor stands on a road centreline and one that every stand keeps an open corridor from the nearest road with 2.5 clear in front, plus a table-wide gait check that fails any figure whose world position travels while its thighs stay locked. That gait check found thirteen: ten paced stand walkers built from the working resident band, which drops the movement observer, and three Mediterranean sailors who stood upright while their boats sailed (now seated at a tiller), and the olive mill's mule got hinged legs. The three Spanish fountains now run (jet, droplets, two falling sheets, a ripple ring). `spainHouse` gained chimneys for andalus, mancha and basque and publishes `userData.smoke`, so nine of the fourteen houses smoke; `worldkit.ts` reads an optional `userData.smokeTint` beside the anchor and gives those sprites a thin grey wisp instead of white steam, leaving untinted sources unchanged. The hórreo landmark was removed at the owner's request, so **Spain's card-only count drops from 15 to 14** and its landmarks from 5 to 4.
- 2026-09-16: Second round of owner feedback on Spain, four items. The four free-standing plaza arcades were removed (a tan cornice slab on grey granite piers, which from the overview read as a bridge half in the water; the one at the river's edge was the "yellow bridge-looking thing", so the hórreo removal in the round before it had been a misidentification, and both are now gone). The six square lamps that hung from their beams stand on iron lamp posts. The decorative barraca in the rice fields went too. Chimney smoke was rebuilt after the owner could not see it: matching China's four white puffs at half alpha was the original instruction and turned out to be invisible at overview zoom in China too, so a tinted source now gets eight puffs at .85 peak alpha rising 3.1 in a wood-smoke grey-white, while untinted sources everywhere else are unchanged, and the anchor is computed from each style's own ridge so it clears the Galician gable. The olive mill's mule circled the stone backwards and unattached; it is now harnessed to the far arm of the beam with a trailing yoke pole and walks a stance-and-swing stride. `spain-world.mjs` gained checks for all of it: nothing but a bridge deck may stand on a road, every square lamp must reach the ground, the arcade, barraca and hórreo must be absent, and the mill mule's head axis must lead its travel with its hooves going backward for most of the stride.
- 2026-09-16: The owner rejected Spain's swaying pepper strings ("it's just not natural"). They were `breeze` crops, which cut the string out of the painting by colour and repair the wall behind it, so the cut edge and the repair showed on every swing. **All eleven Spain breeze crops are gone, so the mask count drops from 41 to 30**, and the audit renders no Spain crop at all. Five room orientations (`es_tortilla` wide and portrait, `es_manchego` wide and portrait, `es_jamon` portrait) now hang the delivered `es-pepper-ristra` sprite over the untouched painting instead, the way the China hotpot room hangs its lanterns; `scene-painted.ts` gained `hung`/`portrait.hung` for that and `scene.ts` re-places a portrait sprite from `paintingFrame` on resize. The other six leave the painted string still because a person crosses it, the frame cuts it, a garlic braid tangles with it, or the wall is in hard sunlight the even-lit sprite cannot match. Four signatures moved off `breeze`: `es_tortilla` to the firebox `light`, `es_gazpacho` to the orange canopy `leaves`, `es_manchego` to `birds` over the windmill ridge, `es_sidreria` to the `stream-glint` on the cider thread; the sidrería door-post lantern and its portrait sky birds came back into the freed slots. `room-loops.mjs` now counts a swaying hung sprite as a loop, and all 66 rooms stay at three or four in both orientations. The idle-motion numbers for `es_gazpacho`, `es_tapas`, `es_pa_tomaquet` and `es_sidreria` are stale until `room-motion.py` is re-run through the world route.
- 2026-09-16: Second note from the owner on the same pass: "it's still weird, there is in the background the same chilli string, the China room doesn't have it." Hanging the sprite over the painted string left the string showing in the notches between the sprite's peppers and at the ends of the swing; the hotpot painting has nothing under its sprite and that is the standard. **Four room JPGs are now derived files**: `scripts/scenes/paint-out-strings.py` keys the string with the engine's own `ristra` rule inside a hand-measured box, closes it into a solid silhouette, protects the hook and every sizeable red neighbour, smears the mask along the painted cast shadow, and fills with `cv2.inpaint` Telea; originals and SHA-256 sidecars go to `.data/originals/`, and the script refuses to run twice over the same file. The four sprites were re-fitted against the cleaned paintings so each rope loop lands on the painted hook and the peppers fill the space the painted string had, which made all four smaller. **`es_jamon` portrait lost its sprite**: its string hangs in front of a curing ham, an iron column and the glazed roof, and no inpaint can clean that, so it keeps a still painted string and takes a third loop from the lit market lantern down the hall instead. `room-loops.mjs` still passes at 66 rooms; `npm test` 16 of 16.
